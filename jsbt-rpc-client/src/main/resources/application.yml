server:
  port: 10021
spring:
  profiles:
    active: dev
grpc:
  client:
    local-grpc-server:
      host: localhost
      port: 8800
      enableKeepAlive: true
      keepAliveWithoutCalls: true
      keepAliveTimeout: 30
